#Phoronix Snap

For use

Create .snap:

```bash
  $ snapcraft
```

Install:
```bash
  $ sudo snap install --devmode phoronix-snap_0.1_amd64.snap
```

Call app phoronix:

```bash
  $ phoronix-snap
```
